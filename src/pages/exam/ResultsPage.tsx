export default function ResultsPage() {
  // En esta versión MVP los resultados se muestran inline.
  // Esta página queda lista para usar state/router si preferís un resumen dedicado.
  return <p>Resultados del examen (próxima iteración).</p>
}
